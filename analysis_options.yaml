# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Treat info level issues as warnings
    missing_return: warning
    missing_required_param: warning
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/generated_plugin_registrant.dart"
    # Vendored plugin example apps are separate Dart packages and shouldn't be
    # analyzed as part of the main app package.
    - "packages/whisper_ggml_plus/example/**"

linter:
  # The lint rules applied to this project can be customized in the
  # section below to disable rules from the `package:flutter_lints/flutter.yaml`
  # included above or to enable additional rules. A list of all available lints
  # and their documentation is published at https://dart.dev/lints.
  rules:
    # === Style rules ===
    prefer_single_quotes: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_final_locals: true
    prefer_final_in_for_each: true
    prefer_relative_imports: true
    
    # === Error prevention ===
    avoid_print: true
    avoid_empty_else: true
    avoid_returning_null_for_void: true
    cancel_subscriptions: true
    close_sinks: true
    
    # === Documentation ===
    public_member_api_docs: false  # Enable if you want to enforce docs
    
    # === Code organization ===
    sort_constructors_first: true
    sort_unnamed_constructors_first: true
    directives_ordering: true
    
    # === Type safety ===
    always_declare_return_types: true
    avoid_dynamic_calls: false  # May be too strict for some use cases
    avoid_types_on_closure_parameters: false
    
    # === Null safety ===
    use_if_null_to_convert_nulls_to_bools: true
    unnecessary_null_checks: true

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
